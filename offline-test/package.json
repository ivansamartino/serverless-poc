{
  "name": "offline-test",
  "version": "1.0.0",
  "description": "Clone the serverless offline working copy in the parent folder of this Git repository to be able to point to it through the local file system",
  "main": "index.js",
  "devDependencies": {
    "serverless-offline": "^12.0.4"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
